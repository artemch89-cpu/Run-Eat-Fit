# CLAUDE.md — Run Eat Fit

Читай этот файл первым при возврате к проекту. Полная спека — в `MVP-SPEC.md`, сырые материалы для методичек — в `research/`.

## Что это

Telegram-бот — персональный AI-тренер с памятью о жизни пользователя. Не калькулятор калорий (ниша занята — Calorie AI, FoodScan, FitnessAI Bot), дифференциация — живой коуч, который держит контекст жизни человека (стресс, перелёт, самочувствие) и подстраивает план под это.

## Статус

MVP-спека готова (07.09.2026). Скелет + анкета (не «онбординг» — так называть при общении с Артёмом) + диалог с коучем готовы (08.09.2026), тестируется живьём на аккаунте Артёма.

`/start` → анкета (цель → профиль нагрузки → стиль общения, кнопки → время чек-ина, текст ЧЧ:ММ) → после завершения обычный текст уходит в диалог с коучем (системный промпт из профиля юзера + текущая дата/время по Белграду, история в таблице `messages`). Ошибка API не роняет бота, юзер видит сообщение о сбое.

09.09.2026, по итогам живого тестирования на аккаунте Артёма, докручено:
- Диалог переведён на Haiku 4.5 (было Sonnet 5) — качество устраивает, дешевле. См. «Модели» ниже.
- Модель иногда игнорировала инструкцию форматирования и слала Markdown вместо Telegram HTML — добавлен программный конвертер `**жирный**`/`*курсив*` → `<b>`/`<i>` в `coach.js` как страховка поверх инструкции в промпте.
- Коуч не знал текущую дату/день недели (путал «завтра») — в системный промпт теперь подставляется реальные дата/день недели/время (`Europe/Belgrade`) на каждый запрос.
- После выдачи плана коуч мягко проговаривает, что план — ориентир, а не то, что нужно запомнить наизусть (сопровождение — каждый день по факту).
- Добавлен маркер `[[SPLIT]]`: если уместно, коуч предлагает список покупок отдельным сообщением — `index.js` режет ответ по маркеру и шлёт как два `sendMessage`.
- **Core loop готов (09.09.2026):** `setInterval` раз в минуту (`checkinTick` в `index.js`) сверяет `checkin_time` пользователей с текущим временем по Белграду, шлёт триггер через `getCheckinTrigger` (специальный маркер `[[DAILY_CHECKIN]]`, модель понимает его как «время написать первой», не как реплику юзера). Поле `last_checkin_date` в `users` бережёт от повторной отправки в ту же дату. Если пользователь не ответил на прошлый чек-ин — видно по истории (два ответа коуча подряд), промпт просит мягко вернуть, без укора.

11.09.2026 — оптимизация промпта + возврат на Haiku 4.5 (Haiku 3-й раз путал день↔тренировку; причина не в модели — раньше код давал ей недельный шаблон + список исключений отдельно и просил совместить на лету):
- `formatPlanSection` теперь сводит календарь в коде: окно 14 дней, одна строка на день, разовые изменения уже наложены на схему, метки СЕГОДНЯ/ЗАВТРА считаются в коде. Модель не вычисляет даты и ничего не сопоставляет.
- `buildDayAnchor` / `appendDayAnchor`: выжимка на 3 дня клеится к последнему сообщению юзера **в вызове API** (не в БД) — последнее, что модель видит перед ответом.
- Порядок системного промпта: `FROZEN_INSTRUCTIONS` (одинаково для всех) вперёд, профиль → тест готовности → календарь → строка «Сейчас:» в самый конец. Готовит почву под prompt-кэш (пока не включён — отдельный заход).
- Модель `claude-sonnet-5` → `claude-haiku-4-5`, `max_tokens` 4000 → 1500, `thinking` убран (у Haiku 4.5 нет `type:disabled`). При обрыве по `max_tokens` без текста — один повтор с 3000, потом ошибка (было: юзер сразу видел «сбой»).
- `coach.js` экспортирует `buildSystemPrompt` / `formatPlanSection` / `buildDayAnchor` для тестов (тест-файла пока нет).
- Фото еды через Haiku 4.5 (vision у неё есть) — не сделано, следующий этап.

11.09.2026 (второй заход) — постоянный журнал тренировок, отдельно от плана. Артём поднял вопрос: нужно помнить всю историю тренировок клиента для анализа прогресса, план (`weekly_plan`/`plan_overrides`) для этого не подходит — он намерение, перезаписывается.
- Новая таблица `training_log` (одна строка на юзера+дату, как `plan_overrides`) — `planned` (код резолвит сам через `resolveDayWorkout`, не спрашивается у модели), `status` (закрытый список `done/partial/skipped/rest/sick` — чтобы потом считать SQL-запросом, не текстовой классификацией через ИИ), `actual`/`note` (свободный текст — нюанс не теряется, просто не участвует в подсчётах).
- Новый tool `log_training_day` — модель вызывает, когда юзер рассказывает факт (сделал/не сделал/частично). Явно разведён с `update_plan` (намерение/будущее) и с предположениями («наверное не успею» — вообще не вызывать).
- **Вечернее закрытие дня** (`dayCloseTick`, 21:00 Белград, фиксированная константа, не поле анкеты): если юзер за день ни разу не упомянул тренировку — пассивная ловля в разговоре не сработает, день молча останется без записи. Второй тик по аналогии с утренним чек-ином — маркер `[[DAY_CLOSE]]`, спрашивает коротко, если `training_log` за сегодня ещё пуст.
- `fitness_tests` — append-only история результатов теста готовности (рядом с существующим `users.fitness_test_*`, который остался снимком последнего теста — не трогали).
- **Явно НЕ в этом заходе:** механика стрика/прогресса (Duolingo-стиль, «сколько дней подряд») — Артём попросил обсудить отдельно. `training_log.status` — ровно те данные, из которых стрик считается одним SQL-запросом, когда договоримся про семантику (входит ли `rest`/`sick`, что показывать и где).
- План реализации — `/home/agent/.claude/plans/dazzling-yawning-sunset.md` (утверждён Артёмом), там же обоснование дизайна.

11.09.2026 (третий заход) — prompt-кэш системного промпта включён:
- `buildSystemPrompt` теперь отдаёт не строку, а массив из 3 блоков с двумя точками кэша (`cache_control: {type:'ephemeral'}`): (1) `FROZEN_INSTRUCTIONS`, (2) профиль+тест готовности+календарь плана, (3) без кэша — строка «Сейчас:» (меняется каждый запрос).
- **Важная находка:** у Haiku 4.5 минимальный кэшируемый префикс — 4096 токенов (у Sonnet/Opus 5 — 512-1024). Сам системный промпт (~3600 токенов) один этого порога не набирает — реально кэшируется только вместе с тремя tool-схемами (`tools` идут в префикс перед `system`), в сумме ~10800 токенов. Без tools в запросе (или если их станет меньше/короче) кэш может снова замолчать — это не баг, а особенность порога у этой модели.
- Живой тест на синтетическом юзере через боевой путь (`getCoachReply`, с tools): 1-й запрос — `cache_creation_input_tokens: 10812`, 2-й запрос тем же юзером — `cache_read_input_tokens: 10812`, `input_tokens` упал с 509 до ~500 (осталось только «Сейчас:» + новое сообщение). TTL — стандартный эфемерный (5 минут), не менял.

`.env` создан Артёмом вручную (не через меня — писать в `.env` мне запрещено технически, даже проверить наличие переменной нельзя). `TELEGRAM_BOT_TOKEN` и `ANTHROPIC_API_KEY` подтверждены рабочими (коуч отвечает).

Тестовый процесс бота — не через systemd, а руками (`setsid nohup node src/index.js`), отвязан от сессии, живёт между разрывами. Есть постоянная permission на рестарт этого процесса в рамках дев-сессии.

**Осторожно при тестировании (урок 11.09.2026):** НИКОГДА не импортировать/запускать `src/index.js` напрямую (`import('./src/index.js')`, `node src/index.js` из тестового скрипта и т.п.), если рабочий процесс бота уже запущен — `bot.launch()` открывает второй `getUpdates`-поллинг на тот же Telegram-токен, Telegram отвечает `409: Conflict`, и это роняет РАБОЧИЙ процесс (необработанное исключение). Для тестов — только импорт функций из `db.js`/`coach.js` напрямую, без `index.js`.

Дальше по плану (по приоритету со слов Артёма, 09.09.2026, дополнено 11.09):
- Механика стрика/прогресса (Duolingo-стиль) — отдельный разговор с Артёмом, данные для неё уже собираются (`training_log`)
- Уточняющий вопрос для цели «Другое» (сейчас в базу пишется буквально слово «другое», коуч не знает, что это); возможность посмотреть/поменять профиль без пересоздания анкеты
- Следующий этап: фото еды (`bot.on('photo')` пока не существует — фото сейчас просто игнорируется без ответа); курируемые skill-методички + цикл проверки Артёмом перед тем, как план строится не с нуля от модели

## Ключевые решения (не пересматривать без веской причины)

- Аудитория: русскоязычные релоканты в Сербии, Telegram-only на старте, сербский язык — фича на потом
- Гипотеза успеха MVP: ежедневное возвращение пользователя к разговору, не оплата (цена — вторичная гипотеза, 300-1000₽/мес)
- **Оплата модели (обновлено 09.09.2026):** решение от 08.09 про подписку/`askModel()` отменено. Заводим платный `ANTHROPIC_API_KEY` сейчас. `src/coach.js` уже вызывает `@anthropic-ai/sdk` напрямую — код менять не нужно, ключ подхватится из `.env` автоматически. Ключ Артём вписывает в `.env` сам (мне запрещено писать туда).
- Core loop: петля привычки — триггер (бот пишет первым) → минимальное действие → награда → план на срыв
- Тренировочные планы: гибрид — 2 профиля нагрузки на старте (выносливость, силовая), skill-методички курирует Артём вручную (веб-поиск по авторитетным источникам, НЕ GitHub), человек в цикле проверки обязателен
- Бот — conditioning/lifestyle-коуч (питание, разминка, восстановление), не тренер по технике конкретного спорта — под это не нужна экспертиза в каждом виде
- Фото еды — в MVP (стандартное разрешение, не high-res), но ещё не реализовано в коде — следующий этап
- **Модели (обновлено 11.09.2026):** Haiku 4.5 для диалога с коучем. Дешевле Sonnet 5 вдвое, плюс с 11.09 работает prompt-кэш (кэш-чтение ~1/10 цены) — см. запись выше. Путаницу день↔тренировка, из-за которой 10.09 откатывались на Sonnet, теперь снимает не модель, а код: сведённый календарь + якорь на 3 дня. Если после живого теста Haiku всё равно поедет на краевых случаях — тогда Sonnet, но сначала фикс (учесть: у Sonnet другой порог кэша — 1024 токена вместо 4096 у Haiku, кэш не сломается). Для фото еды — Haiku 4.5 (vision есть), тест на реальных фото из `research/test-photos/` ещё не делали
- Память: простая БД на юзера, НЕ векторная база, НЕ отдельный сервер на человека
- Оплата в MVP — вручную, переводом, первым 20-30 знакомым

## Явно ВНЕ MVP — не предлагать без явного запроса Артёма

Viber/кроссплатформенность, геймификация с блокировкой приложений (нужно нативное приложение), подключение часов/Strava, автоматическая мультивалютная оплата, полностью автономная генерация плана моделью без ограничения категорий и без методичек

## Оценка сроков

3-4 недели по 1.5 часа в день (~20-25 сессий), при условии не отвлекаться на пункты выше

## Материалы для методичек (research/)

- `deepseek-running-raw.md` — сырой материал по бегу от DeepSeek. Нужны фиксы перед использованием: темпы сделать относительными (не абсолютные минуты/км), сгладить прогрессию недели 2→3 (там нарушено правило +10%/неделю), убрать личные данные Артёма (раздел 5) и «жёсткий тон» (конец раздела 6)
- `coach-feedback-dialogue.md` — разбор диалога с живым тренером. Тренер независимо подтвердил дифференциацию продукта («ИИ не видит контекста, не мотивирует») — использовать как валидацию при разговоре с инвесторами/партнёрами. Плюс: идея контрольных забегов раз в месяц, ценовой ориентир живого тренера (6000₽/мес)
- `dialogue-design-motivation.md` — как мотивировать пользователя делиться жизненным контекстом: видимый результат за каждым рассказом, точечные вопросы вместо опроса, разное обращение с новичками и скептиками
